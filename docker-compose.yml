services:
  aitool:
    build:
      context: .
      dockerfile: Dockerfile
    image: aitool-runtime:local
    container_name: aitool
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.local
    volumes:
      - ../AiTool-content:/AiTool-content:ro
    restart: unless-stopped
